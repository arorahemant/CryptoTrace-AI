# AI architecture

AI is an explanation layer over structured case context. Without an API key, deterministic responses are used. Unsupported identity, criminality, banking, off-case, or future-fact requests return: “Insufficient evidence to determine this confidently.”
