# Graph engine

Wallets are nodes and transactions are directed edges. The backend returns serialized nodes, edges, metadata, and a highest-value primary path for React Flow. Intermediaries are inferred from pass-through structure and centrality.
